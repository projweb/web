<%= form_for(@beer_geek) do |f| %>
  <% if @beer_geek.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_geek.errors.count, "error") %> prohibited this beer_geek from being saved:</h2>

      <ul>
      <% @beer_geek.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
